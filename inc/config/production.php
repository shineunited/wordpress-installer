<?php
/**
 * Configuration overrides for WP_ENV === 'production'
 */

use Roots\WPConfig\Config;

/**
 * You should try to production-specific configuration if possible. However,
 * should you need to, you can always set production configuration values
 * with `Config::define`.
 */
